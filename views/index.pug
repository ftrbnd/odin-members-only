extends layout

block content
  h1= title

  if currentUser
    p Welcome #{currentUser.full_name}!
    p Membership Status: #{currentUser.membership}
    if currentUser.membership !== 'premium'
      button
        a(href='/upgrade') Become a Premium Member
    button
      a(href='/logout') Log Out
  else
    p Welcome to #{title}
    button 
      a(href='/signup') Sign Up
    button
      a(href='/login') Log In